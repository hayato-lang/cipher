<%= form_with(model: [event, comment], url: event_comments_path(@event) ) do |f| %>
    <%= f.text_area :text, placeholder:"コメントする", row:"2", class: "input-mysize" %>
    <%= f.submit "SEND", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>